services:
  - type: web
    name: private-cinema-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    branch: main
    rootDir: .

  - type: web
    name: private-cinema-frontend
    runtime: static
    buildCommand: echo "Static site ready"
    staticPublishPath: .
    branch: main
